# Place PCAP files here for PCAP-mode analysis.

Examples:
- emotet-sample.pcap
- qakbot-sample.pcap

Download sample PCAPs into this directory before running `./setup-zeek-lab.sh` and then use the entrypoint in pcap mode.
